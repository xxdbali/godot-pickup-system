extends TabContainer
func _ready():
    # Set the title for the first tab (index 0)
    set_tab_title(0, "Inventory")

    # Set the title for the second tab (index 1)
    set_tab_title(1, "Guide")

    # Set the title for the third tab (index 2)
    set_tab_title(2, "Options")